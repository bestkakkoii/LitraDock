# Companion notices and native provenance

These unmodified upstream texts accompany deployment preparation separately from both immutable 55-member application archives. `manifest.json` records exact text bytes, pinned upstream commits or archive/member identities. Preserve this directory together with the application and external runtime. Do not insert its files into a frozen archive and reuse the old archive digest.

Included: SQLitePCLRaw2.1.12 Apache license and full upstream NOTICE, PdfPig0.1.14 Apache license and NOTICES, Npgsql10.0.3 PostgreSQL license, EF Core10.0.12 MIT text, .NET apphost10.0.11 MIT and third-party notices, ASP.NET/.NET runtime10.0.12 license and third-party notices, and the original SQLite3.53.3 source dedication comment. The broad upstream SQLitePCLRaw notice also discusses alternative SQLCipher/OpenSSL builds; retaining the complete upstream text does not assert those components are linked in this e_sqlite3 build.

`native-provenance.json` adds evidence without rewriting the earlier inventory: both shipped Linux apphosts exactly equal the official Microsoft.NETCore.App.Host.linux-x6410.0.11 template after the documented application-path substitution. The10.0.12 template was checked and did not match; installed runtime patch and build host pack are separate identities. The pinned10.0.11 package retains its own license/notices. See the upstream [HostWriter implementation](https://github.com/dotnet/runtime/blob/v10.0.11/src/installer/managed/Microsoft.NET.HostModel/AppHost/HostWriter.cs).

The exact packaged Linux x64 SQLite library was loaded on Debian x64 solely to call its version/source-ID functions. It reports3.53.3 and the source ID on the [official release page](https://sqlite.org/releaselog/3_53_3.html). The downloaded original amalgamation's sqlite3.c SHA3-256 also matches that page. SQLite's [upstream rights statement](https://sqlite.org/copyright.html) distinguishes deliverable public-domain code from build tooling. This establishes the observed engine/source identity and preserved upstream notice, not an independently reproduced native build or execution of every bundled RID.

No vulnerability scan, certificate chain/revocation verification, signing, legal ownership determination, alternate commercial license, target rollback or physical-device support follows from this directory. OS packages and PostgreSQL retain their own installed notices; unbundled document-worker assets remain covered by their existing separate inventory. Do not claim ownership over upstream components or research papers.
